# Frontend (Vite + React)

## Quickstart
```bash
cd frontend
npm install
npm run dev
```

## Env
Create `.env` if you want to override API base:
```
VITE_API_BASE=http://127.0.0.1:8000/api/v1
```

## Notes
The old static MVP UI remains in `frontend/public/index.html`.
